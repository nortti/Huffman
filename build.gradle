apply plugin: 'java'
apply plugin:'application'
mainClassName = "Huffman"
run {  
        if ( project.hasProperty("appArgs") ) {  
                    args Eval.me(appArgs)  
                        }  
}
