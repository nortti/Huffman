repositories {
    mavenCentral()
}
apply plugin: 'java'
apply plugin:'application'
task docs(type: Javadoc) {
    source = sourceSets.main.allJava
    destinationDir = reporting.file("../../Dokumentit/Javadocs/")
    }
mainClassName = "huffman/Huffman"
dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.11'
}
